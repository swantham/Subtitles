# Subtitles

Subtitles for Malayalam Films.

The subtitles are stored in [WebVTT](https://en.wikipedia.org/wiki/WebVTT) (.vtt) format (simpler, easier to merge).

To convert to SubRip (.srt), use
`ffmpeg -i name.vtt name.srt`

